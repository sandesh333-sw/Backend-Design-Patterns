/**
 * SideCard pattern
 * Thick clients, Thicker backends
 * 
 * A sidecar is a design pattern where a secondary application or service runs alongside the main application, providing additional functionality or support. The sidecar can be used to offload certain tasks from the main application, such as logging, monitoring, or authentication, allowing the main application to focus on its core responsibilities.
 * 
 * Pros: Separation of concerns, easier maintenance, scalability, can be developed and deployed independently
 * Cons: Increased complexity, potential for communication issues between the main application and the sidecar, may require additional resources
 * 
 * Example use cases: Service mesh (e.g., Istio), logging and monitoring (e.g., Fluentd), authentication and authorization (
 */